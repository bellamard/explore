import { StyleSheet, Dimensions } from 'react-native';
import { Colors } from './logement';
const StylesPublie = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.white,
    padding: 20,
  },
});

export default StylesPublie;
