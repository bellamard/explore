import Styles, { colors } from '../../styles';
import { StyleSheet, Dimensions } from 'react-native';
const { width, height } = Dimensions.get('window');
const StylesApercus = StyleSheet.create({});
export default StylesApercus;
